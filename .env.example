# Backend
DATABASE_URL=sqlite+aiosqlite:///./data/config.db
SECRET_KEY=your-secret-key-change-in-production
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
DEBUG=true

# --- Redis & State Management ---
# REDIS_URL=redis://localhost:6379/0
# TTL for captured records in Redis (in seconds). Default is 14400 (4 hours).
# SYNC_STATE_TTL=14400

# Frontend
# If empty, relative paths will be used (recommended for production with Nginx)
VITE_API_URL=http://localhost:8001
